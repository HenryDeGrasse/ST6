<?xml version="1.0" encoding="UTF-8"?>
<!--
  Structured logging configuration for weekly-service.
  PRD §9.7 & §14: every log entry includes correlationId and orgId.
-->
<configuration>
    <springProfile name="!test">
        <!-- JSON structured logging for non-test environments -->
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <pattern>{"timestamp":"%date{yyyy-MM-dd'T'HH:mm:ss.SSSZ}","level":"%level","logger":"%logger{36}","message":"%replace(%msg){'\"','\\\"'}","correlationId":"%X{correlationId:-}","orgId":"%X{orgId:-}","userId":"%X{userId:-}","thread":"%thread","exception":"%replace(%xException){'\"','\\\"'}"}%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

        <!-- Slow query detection: log queries taking > 100ms -->
        <logger name="org.hibernate.SQL" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
        <logger name="org.hibernate.stat" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
    </springProfile>

    <springProfile name="test">
        <!-- Simple logging for tests -->
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="WARN">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
</configuration>
